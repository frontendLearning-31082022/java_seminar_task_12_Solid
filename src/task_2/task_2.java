package task_2;

import patterns.ImmutableClass;
import patterns.Null_Object;
import patterns.Producer_Consumer.Producer;
import patterns.Producer_Consumer.Producer_Consumer;
import patterns.Strategy;

// Реализуйте паттерны: Null object, ImmutableClass, Стратегия и Producer-Consumer
public class task_2 {
    public static void main(String[] args) {
        Null_Object.main(null);
        ImmutableClass.main(null);
        Strategy.main(null);
        Producer_Consumer.main(null);
    }
}
